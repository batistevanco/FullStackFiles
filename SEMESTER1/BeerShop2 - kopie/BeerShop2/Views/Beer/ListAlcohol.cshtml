@model BeerShop2.ViewModels.BeerSearchByAlcoholVM

@{
        ViewData["Title"] = "GetBeersByAlochol";
}



<h4>BeerSearchByAlcoholVM</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ListAlcohol">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AlocholPercentage" class="control-label"></label>
                <input asp-for="AlocholPercentage" class="form-control" />
                <span asp-validation-for="AlocholPercentage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Zoek" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@if (Model != null)
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    Naam
                </th>
                <th>
                    Brouwernaam
                </th>
                <th>
                    Soort
                </th>
                <th>
                    Alcohol
                </th>
                <th>
                    Image
                </th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Beers!)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BreweryName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.VarietyName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Alcohol)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Image)
                    </td>

                </tr>
            }
        </tbody>
    </table>
}
else  // Beers is een lege lijst
{
    <p>Geen bieren gevonden voor het opgegeven alcoholpercentage.</p>
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

